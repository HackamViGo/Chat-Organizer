# 🧠 Meta-Architect (v3.1) - Strategic Tooling Strategy

> [!CAUTION]
> # ⚖️ ИНСТРУМЕНТАЛЕН ЗАКОН (Meta-Architect v3.1)
> Използването на инструментите по-долу е разрешено **САМО** в рамките на протокола `Audit -> Knowledge -> State -> Verify`. Инструментариумът е подвластен на [SKILL.md](file:///home/stefanov/Projects/Chat%20Organizer%20Cursor/.agent/skills/meta_architect/SKILL.md).

Този документ дефинира подредбата на инструментите през призмата на **Meta-Architect Protocol (v3.1)**. Приоритетите са разпределени според жизнения цикъл на една задача: **Audit -> Knowledge Retrieval -> State Update -> Execution -> Verification**.

---

## 📊 Резюме на архитектурното захранване
*   **CRITICAL (25 Tools)**: Всичко, свързано с Health Score, Graph-RAG и State Verification.
*   **HIGH (20 Tools)**: Инструменти за Builder Agents (UI, Logic, DB) и QA одит.
*   **LOW (20 Tools)**: Помощни функции за история на кода и облачна инфраструктура.

---

## 🔴 CRITICAL (25 Tools)
*Инструменти, без които Meta-Architect НЕ МОЖЕ да гарантира Health Score > 80 и Zero-Hallucination.*

*   **run_command (Built-in)**: Изпълнява `pnpm verify` (Project Health Check) – гръбнакът на Meta-Architect протокола.
*   **sequentialthinking (MCP: Sequential Thinking)**: Основен мисловен двигател за планиране на Triad System взаимодействията.
*   **read_text_file (MCP: Filesystem)**: Чете `knowledge_graph.json` и `agent_states/`, за да установи началната точка на всяка мисия.
*   **resolve-library-id (MCP: Context7)**: Критичен за "Reality Checks" чрез Graph Guardian при работа със стари нодове.
*   **query-docs (MCP: Context7)**: Осигурява "Grounding" за Builder агентите при работа с React/Supabase.
*   **search (MCP: Google Dev Knowledge)**: Единственият начин за Meta-Architect да валидира Chrome API интеграциите.
*   **retrieve (MCP: Google Dev Knowledge)**: ВземаMarkdown за инжектиране в контекста на под-агентите (Knowledge Injection).
*   **edit_file (MCP: Filesystem)**: Управлява атомарните промени в `agent_states/*.yml`, за да следи прогреса на Builder-ите.
*   **write_file (MCP: Filesystem)**: Създава нови нодове в графа и актуализира `remediation_plan.yml` след одит.
*   **grep_search (Built-in)**: Сканира проекта за забранени функции (напр. `console.log`) по време на Compliance Check.
*   **view_file (Built-in)**: Валидира физическото съществуване на код срещу `node_id` в архитектурния граф.
*   **replace_file_content (Built-in)**: Извършва прецизните корекции при засичане на архитектурно отклонение.
*   **execute_query (MCP: Supabase)**: Позволява на DB Architect да верифицира RLS политиките директно в базата.
*   **list_tables (MCP: Supabase)**: Валидира дали физическата схема съответства на `DATA_SCHEMA.md`.
*   **get_table_schema (MCP: Supabase)**: Осигурява типовата стабилност на знанието в `shared` пакетите.
*   **task_boundary (Built-in)**: Осигурява прозрачност на процеса спрямо User-а (Commander Interaction).
*   **notify_user (Built-in)**: Единственият легален начин за ескалация при Health Score < 80.
*   **list_dir (Built-in)**: Валидира структурата на monorepo-то съгласно `monorepo_structure` закона.
*   **find_by_name (Built-in)**: Търси критични конфигури за Meta-Architect (`main.md`, `SKILL.md`).
*   **command_status (Built-in)**: Следи резултата от `pnpm install` и `pnpm verify`.
*   **view_file_outline (Built-in)**: Извлича сигнатурите на функциите за Knowledge Retrieval фазата.
*   **multi_replace_file_content (Built-in)**: Използва се за масово обновяване на лицензи или архитектурни хедъри.
*   **open_nodes (MCP: Memory)**: Отваря конкретни възли в графичната памет за установяване на контекст.
*   **search_nodes (MCP: Memory)**: Търси семантично свързани знания за инжектиране в под-агентите.
*   **list_resources (MCP: General)**: Проверява наличността на инструменти преди стартиране на Triad System цикъла.

---

## 🟡 HIGH (20 Tools)
*Инструменти за Execution и QA фазите на продукта.*

*   **playwright (MCP: Playwright)**: Използва се от QA Examiner за Blind Audits на функционалността.
*   **browser_subagent (Built-in)**: Делегира се за рендериране на сложни UI състояния в Dashboard-а.
*   **inspect_node (MCP: Chrome DevTools)**: Одит на разширението за сигурност – проверява за "No window exposure".
*   **execute_js (MCP: Chrome DevTools)**: Симулация на потребителски действия за стрес-тест на Extension-а.
*   **create_entities (MCP: Memory)**: Записва нови архитектурни "истини" след успешен Health Gate преминаване.
*   **read_graph (MCP: Memory)**: Анализира йерархията на зависимостите преди голям преход (Migration).
*   **get_markdown (MCP: Fetch)**: Извлича документация от уеб за обновяване на External Knowledge Graph.
*   **get_rendered_html (MCP: Fetch)**: Проверява конкурентни UI патърни за вдъхновение (Research).
*   **search_code (MCP: Github)**: Търси за нарушени "Security Laws" (напр. хардкоднати localhost) в историята.
*   **get_file_contents (MCP: Github)**: Сравнява локалния код с "Truth" версията в репозиторито.
*   **create_pull_request (MCP: Github)**: Финализира ескалацията на промени към Commander-а.
*   **generate_image (Built-in)**: Създава UI активи, които се вписват в "Premium Design" изискванията.
*   **read_url_content (Built-in)**: Обучение на агента върху нови уеб стандарти.
*   **capture_screenshot (MCP: Chrome DevTools)**: Визуално потвърждение на дизайна в Walkthrough-а.
*   **list_issues (MCP: Github)**: Синхронизира текущите задачи със статуса в GitHub.
*   **search_files (MCP: Filesystem)**: Намира забравени скриптове в `tmp` или скрити директории.
*   **get_file_info (MCP: Filesystem)**: Проверява размера на бъндълите на разширението за оптимизация.
*   **add_observations (MCP: Memory)**: Записва логове от одит фазата за бъдещи корекции.
*   **search_web (Built-in)**: Използва се само при пълна липса на знание в External Graph.
*   **get_deployment (MCP: Vercel)**: Потвърждава, че Dashboard-а е в актуален статус след фикс.

---

## 🟢 LOW (20 Tools)
*Помощни инструменти и такива с ниско влияние върху архитектурния интегритет.*

*   **list_projects (MCP: Vercel)**: Управление на проекти извън текущия фокус.
*   **create_deployment (MCP: Vercel)**: Ръчно стартиране на билд (може и през GitHub PR).
*   **get_graph (MCP: GitKraken)**: Визуална проверка на Git дървото – чисто козметично.
*   **open_repo (MCP: GitKraken)**: Външен интерфейс за Git операции.
*   **create_patch (MCP: GitKraken)**: Генериране на легаси пачове.
*   **create_branch (MCP: Github)**: Ретинно създаване на нови клонове.
*   **move_file (MCP: Filesystem)**: Изключително рядко се ползва в стабилна архитектура.
*   **get_raw_text (MCP: Fetch)**: Четене на CSV/Logs.
*   **+ още 12 специфични функции** за Git, Vercel и конфигурация на локалната машина.

---

**Общо индивидуални инструменти:** ~65
**Основание (Ratio):** ВСЕКИ детайл в Критичната секция съответства на стъпка от **Section 6 (Exit Protocol)** или **Section 3 (Monorepo Structure)** на Meta-Architect закона.
**Статус:** 🟢 Протоколът е напълно покрит технологично.
