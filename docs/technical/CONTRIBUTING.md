# Contributing to BrainBox

**Version**: 3.1.0 (2026-02-10)
**Operational Kernel**: Meta-Architect (v3.1)

Следваме стриктни инженерни стандарти, за да поддържаме здравословен, типизиран и високопроизводителен monorepo проект.

## 🛠️ Работен процес (Development Workflow)

### Предварителни условия
- **pnpm**: Използваме само `pnpm`. Не използвайте `npm` или `yarn`.
- **Node.js**: LTS версия (виж `engines` в `package.json`).

### Основни команди
- **Инсталация**: `pnpm install`
- **Разработка**: `pnpm dev` (стартира Dashboard и Extension в паралел)
- **Верификация**: `pnpm verify` (изчислява Health Score)
- **Билд**: `pnpm build` (използва Turborepo кеширане)

---

## 🛡️ Стандарти за кодиране (Coding Standards)

### 1. Стриктен TypeScript
- **No `any`**: Използването на `any` е абсолютно забранено.
- **Shared Types**: Всички споделени типове трябва да се дефинират в `@brainbox/shared`.
- **Validation**: Използвайте `@brainbox/validation` за Zod схеми.

### 2. Логване и Конфигурация
- **NO `console.log`**: Забранено в производствена среда. Използвайте `logger.debug()` от `src/lib/logger.ts`.
- **NO Hardcoded Strings**: Всички API адреси и секрети трябва да идват от `config.ts` или `.env`.

### 3. Архитектура на компонентите
- **Zustand**: Използвайте магазина за състояние с `useShallow` за по-добра производителност.
- **Isomorphic Logic**: Дръжте бизнес логиката отделена от UI компонентите.

---

## 🔒 Health Gate (Портиер на качеството)

Всеки PR трябва да преминава през автоматизирания скрипт `pnpm verify`.

**Изискване**: Трябва да постигнете **Health Score > 80**, за да бъде одобрен кодът.

| Нарушение | Наказание (Точки) |
|-----------|-------------------|
| `console.log` | -5 pts |
| `any` usage | -10 pts |
| Test failure | -20 pts |
| Lint error | -5 pts |

---

## 🚀 Конвенция за Commits

Използваме Conventional Commits:
- `feat`: Нова функционалност
- `fix`: Поправка на бъг
- `refactor`: Рефакторинг (без промяна във функционалността)
- `docs`: Документация
- `chore`: Поддръжка (dependencies, scripts)

---

## ⚡ Turborepo Pipeline

- Използваме Turborepo за интелигентно кеширане.
- Ако срещнете странни проблеми с типовете, пуснете `pnpm clean` и след това отново `pnpm install`.

---
*Приели сте ролята на Builder под командването на Meta-Architect. Спазвайте протокола.*
