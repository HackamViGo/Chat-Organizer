# Пълен пътеводител на проекта BrainBox (Project Structure Guide)

Този документ служи като наръчник за структурата на проекта, описващ предназначението на всяка папка и файл.

---

## 📂 Основни папки (Root Directories)

| Папка | Описание и функция |
| :--- | :--- |
| **`.agent`** | Конфигурации и специфични инструкции за AI агентите, работещи по проекта. |
| **`.git`** | Вътрешни данни на Git за проследяване на версиите (скрита папка). |
| **`.next`** | Автоматично генерирана папка с компилирания код на Dashboard-а. |
| **`.workflows`** | Дефиниции за автоматизирани процеси и работни потоци. |
| **`AI Knowlage`** | "Мозъкът" на проекта. Съдържа знания за архитектурата, скриптове за одит и Knowledge Graph. |
| **`docs`** | Централното място за документация – за потребители, разработчици и AI агенти. |
| **`extension`** | Целият изходен код на Chrome разширението (Background, Content scripts, UI). |
| **`node_modules`** | Всички инсталирани външни библиотеки (генерира се автоматично от `npm install`). |
| **`public`** | Статични ресурси за уеб приложението (логотипи, икони, шрифтове). |
| **`scripts`** | Помощни скриптове за разработка и поддръжка. |
| **`src`** | Изходният код на Next.js приложението (Dashboard / Табло за управление). |
| **`tests`** | Пълният тестов пакет (Unit, Integration, E2E и браузърни среди). |

---

## 🧩 Подробен преглед на ключовите папки

### 🛠️ `extension/` (Chrome Extension)
*   **`background/`**: Скриптове, които работят в бекграунд (Service Workers), улавят токени и управляват менютата.
*   **`content/`**: Скриптове, които се „инжектират“ в сайтовете (ChatGPT, Claude, Gemini) за улавяне на данни.
*   **`lib/`**: Споделена логика, схеми и нормализатори за превръщане на чатовете в чист формат.
*   **`ui/`**: Кодът за изскачащия прозорец (Popup), който виждате, когато кликнете на иконата.

### 🌐 `src/` (Web Dashboard)
*   **`app/`**: Всички страници и маршрути (routes) на сайта (Табло, Настройки, История).
*   **`components/`**: Всички UI елементи (бутони, карти, менюта), изградени с React.
*   **`lib/`**: Логика за връзка с базата данни, API за съхранение и помощни функции.
*   **`store/`**: Управление на „паметта“ (състоянието) на приложението чрез Zustand.

### 🧠 `AI Knowlage/`
*   **`knowledge_graph.json`**: Карта на технологиите и концепциите, използвани в проекта.
*   **`project_auditor.py`**: Умен скрипт, който проверява качеството на кода и съответствието с плановете.

---

## 📄 Основни файлове (Root Files)

| Файл | Описание | Важност |
| :--- | :--- | :--- |
| **`.env`** | Тайни ключове (Supabase, Gemini). | **КРИТИЧЕН** |
| **`.cursorrules.md`** | Главна „конституция“ на проекта за AI. | **КРИТИЧЕН** |
| **`package.json`** | Сърцето на проекта – дефинира зависимостите и командите. | **КРИТИЧЕН** |
| **`next.config.js`** | Настройки на уеб сървъра. | **ВИСОКА** |
| **`tsconfig.json`** | Настройки на езика TypeScript. | **ВИСОКА** |
| **`tailwind.config.ts`** | Дефинира цветовете и дизайна (CSS). | **СРЕДНА** |
| **`README.md`** | Обща информация за проекта. | **ИНФО** |

---
**Съвет:** Ако не си сигурен къде да сложиш нов файл, обикновено `src/` е за уеб частта, а `extension/` за браузърната част.
