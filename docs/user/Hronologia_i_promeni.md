# Хронология на промените

## 30 януари 2026

### 🚀 Основен фокус: Премахване на Hover Buttons и финализиране на Context Menu стратегията

- **Пълно премахване на Hover Buttons**: Изтрита е логиката за `MutationObserver` и `injectUI` от всички контент скриптове (`brainbox_master.js`, `content-chatgpt.js`, `content-claude.js`).
- **Преход към Context Menu**: Дефинирано като единствен метод за взаимодействие. Актуализиран `service-worker.js` за управление на събитията от десния бутон.
- **Обновен UI на разширението**:
  - `lib/ui.js`: Актуализирани Modal и Toast компоненти с по-добри стилове и икони.
  - `popup.html/js`: Оптимизиран интерфейс на попъпа.

### 🧪 Тестване и Валидация
- **E2E Тестове**: Пренаписан `tests/e2e/extension.spec.ts`. Премахнати тестове за бутони, добавени нови за валидация на CSS инжекция на Modal/Toast компонентите.
- **Синхронизация**: Актуализиран `scripts/validate_extension_sync.js` за по-стабилна проверка на данните в Supabase.
- **Документация на скриптовете**: Обновен `scripts/README.md` с описание на всички инструменти.

### 📚 Системна документация и Архитектура
- **Архитектурен ъпдейт**: Ревизирани `SYSTEM_ARCHITECTURE.md` и `Specification.md` — замяна на `MutationObserver_Controller` с `Context_Menu_Handler`.
- **Master TODO**: Маркирани като завършени всички задачи по прехода към контекстно меню в `MASTER_TODO.md`.
- **Одит на AI Knowlage**:
  - Анализирана Meta-Architect+ системата.
  - Създаден `AI Knowlage/README.md`.
  - Потвърдено: **Supabase** е активният backend; Firebase остава само като референция.

### 🛠️ Инфраструктура и Cleanup
- **Dev инструменти**: Актуализиран `dev/setup-agent-system.sh` (премахнати hover menu референции в генерираната документация).
- **Масово почистване**: Изтрити над 15 остарели `.md` файла в `docs/project/archive/` и `docs/user/` за намаляване на шума при работа с AI модели.
- **Manifest V3**: Проверена и потвърдена пълна съвместимост на всички промени.

### 🌐 Web App (Frontend/API)
- Синхронизирани страници `src/app/` (Chats, Folders, Auth) с промените в API и структурата на данните.
